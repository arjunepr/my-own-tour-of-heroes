.content
  h1, h2, .header
    text-align center
    transition all 0.5s ease

    &:hover
      letter-spacing 2px
      word-spacing 4px

  font-family Arial, Helvetica, sans-serif
  color #333
  letter-spacing 1px
  line-height 1.5
  max-width 70%
  margin 5% auto
  background-color #ddd
  padding 2rem
  max-width 600px